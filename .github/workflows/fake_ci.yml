on:
  push:
    branches:
      - master
    paths:
      - "**"
      - "!pacakges/server/**"

name: Fake-CI

jobs:
  test:
    name: Echo
    runs-on: ubuntu-latest
    steps:
      - run: echo FAKE
